/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package DES_logic;

/**
 *
 * @author Achala PC
 */
public class test {
    
        // Converts the input hexadecimal key into its binary representation.
//    private void convertInputKey(String key) {
//        String convertedKey = new BigInteger(key, 16).toString(2);
//
//        // Append the missing leading zeros if required.
//        int lengthDifference = 64 - convertedKey.length();
//        StringBuilder leadingZeros = new StringBuilder();
//        if (lengthDifference > 0) {
//            for(int num = 1; num <= lengthDifference; ++num) {
//                leadingZeros.append("0");
//            }
//        }
//        convertedKey = leadingZeros + convertedKey;
//        BinaryCovertedKey = convertedKey;
//    }
}

    // && key.matches("[0-9A-Fa-f]+")
//    private boolean validateAndSetKey(String key) {
//        if (key.length() == 8) {
//            this.key = key;
//            //get the binary equivalent in textual representatiion
//            String convertedKey = converter.stringToBinary(key);
            // Append the missing leading zeros if required.
//            int lengthDifference = 64 - convertedKey.length();
//            StringBuilder leadingZeros = new StringBuilder();
//            if (lengthDifference > 0) {
//                for(int num = 1; num <= lengthDifference; ++num) {
//                    leadingZeros.append("0");
//                }
//            }
//            convertedKey = leadingZeros + convertedKey;
//            BinaryCovertedKey = convertedKey;
//            return true;
//        }
//        else {
//            return false;
//        }
//    }
